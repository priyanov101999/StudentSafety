let express,bodyParser,http,cors,routes;_71c‍.x([["io",()=>io]]);_71c‍.w("express",[["default",["express"],function(v){express=v}]]);_71c‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_71c‍.w("http",[["default",["http"],function(v){http=v}]]);_71c‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_71c‍.w("./src/routes/Routes",[["default",["routes"],function(v){routes=v}]]);// Import required modules






// Create express and http servers
const app = express();
app.use(bodyParser.urlencoded({ extended: false }));
app.use(bodyParser.json());
app.use(cors());
const server = http.createServer(app);
const socketUtils = require("./src/utils/socketUtils");
       const io = socketUtils.sio(server);
socketUtils.connection(io);

const PORT = process.env.PORT || 4000;
initializeApplication();

module.exports = app;

async function initializeApplication() {
  routes(app);
  server.listen(PORT, () => {
    console.log("App Started!!");
  });
  return;
}

// const express = require("express");
// const http = require("http");
// const socketio = require("socket.io");
// import bodyParser from "body-parser";
// const app = express();
// app.use(bodyParser.urlencoded({ extended: false }));
// app.use(bodyParser.json());
// const server = http.createServer(app);
// const io = socketio(server, {
//   transports: ["polling"],
//   cors: {
//     origin: "*",
//   },
// });

// io.on("connection", (socket) => {
//   console.log("a user connected");

//   socket.on("disconnect", () => {
//     console.log("user disconnected");
//   });

//   socket.on("chat message", (msg) => {
//     console.log("message: " + msg);
//     io.emit("chat message", msg);
//   });
// });

// const port = process.env.PORT || 4000;
// server.listen(port, () => {
//   console.log(`Server listening on port ${port}`);
// });
