let MODEL,ResponseUtil;_be5‍.x([["createParams",()=>createParams],["getParams",()=>getParams]]);_be5‍.w("../constants/Model.js",[["MODEL",["MODEL"],function(v){MODEL=v}]]);_be5‍.w("../utils/ResponseUtil.js",[["default",["ResponseUtil"],function(v){ResponseUtil=v}]]);

       const createParams = async (req, res, next) => {
  try {
    let data = req.body;
    let model = req.params.model;
    await MODEL[model]
      .query()
      .upsertGraphAndFetch(data)
      .then((data) =>
        ResponseUtil.success(data, 201, `${model} created successfully`, res)
      );
  } catch (error) {
    ResponseUtil.failure(error.name, error.statusCode, res);
  }
};

       const getParams = async (req, res, next) => {
  try {
    let model = req.params.model;
    await MODEL[model]
      .query()
      .then((data) =>
        ResponseUtil.success(data, 200, `${model} fetched successfully`, res)
      );
  } catch (error) {
    ResponseUtil.failure(error.name, error.statusCode, res);
  }
};
